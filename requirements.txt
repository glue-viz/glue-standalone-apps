pyinstaller

# For now pin echo to <0.11 as a change in behavior in that version
# made it so that glue-vispy-viewers crashes instead of emitting
# an exception but proceeding
echo<0.11

glue-core>=1.24.0
glue-qt>=0.3.1
glue-vispy-viewers>=1.3.0
glue-wwt>=0.7.2
glue-astronomy[hips]>=0.14
glue-plotly>=0.13.0
glue-ar>=0.6.0
pvextractor>=0.4
pyavm
astrodendro
pywwt
notebook
# PySide6 (LGPL) rather than PyQt6 (GPL) so the bundle can be distributed on the
# Mac App Store. The PySide6 meta-package pulls in PySide6-Essentials and
# PySide6-Addons, the latter providing QtWebEngine (needed by glue-wwt).
PySide6==6.6.*
vispy==0.15.*

# Pin matplotlib <3.10.9 so as to avoid an incompatibility with pyinstaller
matplotlib<3.10.9
