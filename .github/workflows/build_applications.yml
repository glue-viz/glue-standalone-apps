name: Build Applications

on:
  push:
    branches:
    - master
    tags:
    - '*'

jobs:
  build_applications:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-10.15, ubuntu-18.04, windows-2016]
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9
    - name: Install pyinstaller
      run: pip install git+https://github.com/pyinstaller/pyinstaller
    - name: Install developer version of vispy
      run: pip install git+https://github.com/vispy/vispy
    - name: Install old version of ipykernel to avoid terminal issues
      run: pip install "ipykernel<6"
    - name: Install glue
      run: pip install pip install git+https://github.com/Carifio24/glue@polar-fix glue-vispy-viewers glue-wwt PyQt5==5.14.2 PyQtWebEngine==5.14.0
    - name: Run pyinstaller
      run: pyinstaller glue_app.spec
    - name: Prepare MacOS X application and make dmg
      if: matrix.os == 'macos-10.15'
      run: osx/make_dmg.sh
    - name: Install awscli
      run: pip install awscli
    - name: Upload to S3
      shell: bash
      run: aws s3 cp * s3://glue-installers-exp/
      working-directory: dist
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
