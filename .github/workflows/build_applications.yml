name: Build Applications

on:
  push:
    branches:
    - master
    tags:
    - '*'

jobs:
  build_applications:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-10.15, ubuntu-18.04, windows-2016]
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install pyinstaller
      run: pip install git+https://github.com/pyinstaller/pyinstaller
    - name: Install old version of ipykernel to avoid terminal issues
      run: pip install "ipykernel<6"
    - name: Install old version of astropy to avoid astropy.units issue
      run: pip install "astropy<4.3"
    - name: Install developer version of pywwt
      run: pip install git+https://github.com/WorldWideTelescope/pywwt
    - name: Install glue
      run: pip install git+https://github.com/Carifio24/glue@polar-fix glue-vispy-viewers glue-wwt PyQt5==5.14.2 PyQtWebEngine==5.14.0
    - name: Install plugins
      run: pip install git+https://github.com/glue-viz/glue-plotly git+https://github.com/jk31768/glue-statistics
    - name: Run pyinstaller
      run: pyinstaller glue_app.spec
    - name: Prepare MacOS X application and make dmg
      if: matrix.os == 'macos-10.15'
      run: osx/make_dmg.sh
    - name: Install awscli
      run: pip install awscli
    - name: Upload to S3
      shell: bash
      run: aws s3 cp * s3://glue-installers-exp/
      working-directory: dist
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
